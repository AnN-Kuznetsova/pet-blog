enum ModalButtonControlsType {
  CHANGE,
  SAVE,
  SEND,
  CANCEL,
}

enum PostDateMode {
  TODAY = `TODAY`,
  IN_FUTURE = `IN_FUTURE`,
}

enum DateMeasureType {
  HOUR = `hours`,
  DAY = `days`,
  WEEK = `weeks`,
  MONTH = `months`,
}

enum SnackbarType {
  SUCCESS = `SUCCESS`,
  ERROR = `ERROR`,
}


export {
  DateMeasureType,
  ModalButtonControlsType,
  PostDateMode,
  SnackbarType,
};
