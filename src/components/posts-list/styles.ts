const containerStyles = () => ({
  height: `60%`,// `100%`, // "calc(100% - 50px)",
  overflowY: `auto`,
});

const postsListStyles = () => ({
  padding: 0,
  overflowY: `auto`,
});


export const styles = {
  containerStyles,
  postsListStyles,
};
