const containerStyles = () => ({
  height: `100%`,
  overflowY: `auto`,
});

const postsListStyles = () => ({
  padding: 0,
  overflowY: `auto`,
});


export const styles = {
  containerStyles,
  postsListStyles,
};
