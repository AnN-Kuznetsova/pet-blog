import React from "react";

import { THEME } from "../../helpers/theme";


const rootAppLinkButton = {
  width: "max-content",
} as React.CSSProperties;

const rootAppLink = {
  color: THEME.palette.text.primary,
} as React.CSSProperties;


export const styles = {
  rootAppLinkButton,
  rootAppLink,
};
