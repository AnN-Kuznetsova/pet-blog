const userInfo = () => ({
  display: `flex`,
  alignItems: `flex-end`,
});

const postInfo = () => ({
  marginTop: 7,
});

const date = () => ({
  fontSize: 14,
});

const title = () => ({
  marginBottom: 3,
  textTransform: `capitalize`,
});


export const styles = {
  userInfo,
  postInfo,
  date,
  title,
};
