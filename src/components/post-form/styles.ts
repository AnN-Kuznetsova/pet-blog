const container = () => ({
  width: "60vw",
  paddingTop: 1,

  "& form": {
    display: "flex",
    flexWrap: "wrap",
  },
});

const wrapper = () => ({

});

const control = () => ({
  width: "100%",
  marginBottom: 5,
});


export const styles = {
  container,
  wrapper,
  control,
};
