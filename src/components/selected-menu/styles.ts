const icon = () => ({
  marginRight: 1,
  height: `24px`,
});

const list = () => ({
  padding: 0,
});

const listItem = () => ({
  padding: 0,
});


export const styles = {
  icon,
  list,
  listItem,
};
