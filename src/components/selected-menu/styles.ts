const icon = () => ({
  marginRight: 1,
  height: `24px`,
});

const buttonLabel = () => ({
  minWidth: 24,
});

const list = () => ({
  padding: 0,
});

const listItem = () => ({
  padding: 0,
});


export const styles = {
  icon,
  buttonLabel,
  list,
  listItem,
};
