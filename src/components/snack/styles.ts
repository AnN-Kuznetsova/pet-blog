const snackbarStack = () => ({
  position: "absolute",
  top: 0,
  right: 0,
  paddingLeft: 3,
  paddingBottom: 3,
  overflow: "hidden",
});

const snackbar = () => ({
  position: "relative",
});


export const styles = {
  snackbarStack,
  snackbar,
};
